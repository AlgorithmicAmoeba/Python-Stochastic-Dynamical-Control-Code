# Controller using the linear reactor model measuring both concentration and temperature.

import closedloop_scenarios_single.lin_mpc_var_conf

closedloop_scenarios_single.lin_mpc_var_conf.main(nine=99, linear=True, pf=True, numerical=True)
